{
	"name": "@0655-dev/empty-package",
	"version": "1.0.0",
	"private": true,
	"files": [
		"dist"
	],
	"main": "dist/index.js",
	"devDependencies": {
	},
	"scripts": {
		"test": "rm -rf ./dist && mkdir ./dist && cp ./package-bootstrap.sh ./dist/ && cd ./dist && ./package-bootstrap.sh && make test"
	}
}
